{
  "version": 2,
  "buildCommand": "cd client && npm install --legacy-peer-deps --no-audit --no-fund --silent && npm run build",
  "outputDirectory": "client/build",
  "installCommand": "npm install --no-audit --no-fund && cd client && npm install --no-audit --no-fund",
  "framework": "create-react-app",
  "nodeVersion": "18.x",
  "functions": {
    "api/index.js": {
      "runtime": "@vercel/node@3.0.7"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "/client/build/$1"
    }
  ]
}